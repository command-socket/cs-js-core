/*
 *	Created by Trevor Sears <trevorsears.main@gmail.com>.
 *	4:04 PM -- October 21st, 2019.
 *	Project: @command-socket/core
 */

// import { CommandRegistry } from "../../command/command-registry";
// import { AddCommand } from "./add-command";
// import { TestCommandSet } from "./test-command-set";

const main: () => Promise<void> = async (): Promise<void> => {
	
	// let registry: CommandRegistry<TestCommandSet> = new CommandRegistry<TestCommandSet>(
	// 	new AddCommand()
	// );
	//
	// let result: number = registry.execute("add", [1, 2, 3], undefined);
	//
	// console.log(result);
	
};

main();